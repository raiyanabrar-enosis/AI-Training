# Intel Arc (XPU) GPU builds of torch; falls back to PyPI on other hardware
--extra-index-url https://download.pytorch.org/whl/xpu
torch
torchvision
torchaudio

# Gemini SDK — embeddings and generation
google-genai

# Vector database client
qdrant-client

# PDF text extraction (imported as `fitz`)
pymupdf
pypdf

# Retry/backoff on rate limits
tenacity

# Loads .env
python-dotenv

fastapi
uvicorn[standard]
sentence-transformers

langchain-text-splitters

pageindex

psycopg2
numpy

# Hybrid search — BM25 sparse vectors
fastembed

# Vector optimization — PCA variant
scikit-learn

# TurboVec — quantized ANN index for large embeddings
turbovec