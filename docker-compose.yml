services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"   # REST + the client library
      - "6334:6334"   # gRPC (optional, faster)
    volumes:
      - ./qdrant_storage:/qdrant/storage